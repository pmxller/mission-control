# Turtok Mission Control Dashboard

Modern, clean single-file dashboard UI with:

- Kanban + list views
- Glassmorphism cards with depth/shadow
- Premium button styles + smooth hover interactions
- Auto-refresh (30s) and manual refresh

## Start

Open `index.html` in browser (or serve this folder with any static web server).

Data source: `data.json`

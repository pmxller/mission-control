<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> · Dennis Dashboard</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header class="topbar">
    <div>
      <h1>Dennis Dashboard</h1>
      <% if (user) { %>
        <p class="muted">Eingeloggt als <strong><%= user.username %></strong> (<%= user.role %>)</p>
      <% } %>
    </div>
    <div class="topbar-actions">
      <button type="button" id="themeToggle" class="btn ghost">🌙/☀️</button>
      <% if (user) { %>
      <form action="/logout" method="POST">
        <button class="btn">Logout</button>
      </form>
      <% } %>
    </div>
  </header>
  <main class="container">
